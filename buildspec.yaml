version: 0.2

phases:
  pre_build:
    commands:
      - npm install -g yarn
      - yarn install
  build:
    commands:
      - yarn run build
      - aws s3 cp dist/britecore-ui-plugins.js "s3://$S3_BUCKET" --acl=public-read
cache:
  paths:
    - node_modules/**/*
